<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Projeto Z - Post de Segurança</title>
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Postagem</h1>
        <a href="/users/login" class="btn btn-danger mb-3">Sair</a>
        
        <h2>Criar Nova Postagem</h2>
        <form enctype="multipart/form-data" method="POST" action="/users/create">
            <div class="form-group">
                <label for="titulo" class="form-label">Título</label>
                <input type="text" class="form-control" id="titulo" name="titulo" required>
            </div>
            <div class="form-group">
                <label for="descricao" class="form-label">Descrição</label>
                <textarea class="form-control" id="descricao" name="descricao" required></textarea>
            </div>
            <div class="mb-3">
                <label for="imageUrl" class="form-label">Selecionar Imagem</label>
                <input type="file" class="form-control" id="imageUrl" name="imageUrl" accept="image/*" required>
            </div>
            <button type="submit" class="btn btn-primary">Criar Postagem</button>
        </form>

        <h2 class="mt-5">Minhas Postagens</h2>
        <div class="row">
            <% if (posts.length > 0) { %>
                <% posts.forEach(post => { %>
                <div class="col-md-4 mb-4">
                    <div class="card">         
                        <img src="<%= post.imageUrl %>" class="card-img-top" alt="Imagem da Postagem">
                        <div class="card-body">
                            <h5 class="card-title"><%= post.titulo %></h5>
                            <p class="card-text"><%= post.descricao %></p>
                            <a href="#" class="btn btn-primary">Saiba Mais</a>
                            <!-- Alterar aqui para incluir o ID da postagem -->
                            <a href="/users/postagem/<%= post.id %>" class="btn btn-danger mb-3">Deletar</a>
                        </div>
                    </div>
                </div>
                <% }); %>
            <% } else { %>
                <p>Nenhuma postagem encontrada.</p>
            <% } %>
        </div>
        <a href="/users/login" class="btn btn-secondary mt-3">Retornar</a>
    </div>

    <!-- Bootstrap JS CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
